# Registers DeterministicLanguageExtension (see META-INF/services) for every test, so the
# active language never depends on which class ran before or on leftover persisted state.
junit.jupiter.extensions.autodetection.enabled=true
