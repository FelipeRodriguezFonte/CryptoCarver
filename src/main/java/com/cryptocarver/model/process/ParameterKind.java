package com.cryptocarver.model.process;

/**
 * Declares the visual and behavioral shape of a node configuration parameter.
 */
public enum ParameterKind {
    TEXT,
    MULTILINE,
    NUMBER,
    HEX,
    COMBO,
    CHECKBOX,
    PASSWORD,
    FILE_OPEN,
    FILE_SAVE
}
